# ── Staging Environment Inventory ──────────────────────────────────────
# VPN subnet: 10.20.0.0/24  (WireGuard wg0)

all:
  hosts:
    remote-dev:
      ansible_host: 10.10.20.10
      ansible_user: ansible
      ansible_port: 22
