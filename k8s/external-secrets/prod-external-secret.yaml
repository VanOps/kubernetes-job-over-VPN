---
# ══════════════════════════════════════════════════════════════════════
# ExternalSecrets – PRODUCTION namespace
# HashiCorp Vault KV v2 mount "ansible-vpn", paths:
#   prod/wireguard-config  → { "wg0.conf": "<content>" }
#   prod/vault-password    → { "vault-password": "<pass>" }
#   prod/ssh-key           → { "ssh-private-key": "<pem>" }
# ══════════════════════════════════════════════════════════════════════

# ── WireGuard VPN config ─────────────────────────────────────────────
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: vpn-wireguard-config
  namespace: ansible-jobs-prod
  labels:
    app.kubernetes.io/part-of: ansible-gitops
    environment: prod
spec:
  refreshInterval: 30m
  secretStoreRef:
    name: hashicorp-vault
    kind: ClusterSecretStore
  target:
    name: vpn-wireguard-config
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/part-of: ansible-gitops
          environment: prod
  data:
    - secretKey: wg0.conf
      remoteRef:
        key: prod/wireguard-config
        property: wg0.conf

---
# ── Ansible-vault password ────────────────────────────────────────────
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ansible-vault-password
  namespace: ansible-jobs-prod
  labels:
    app.kubernetes.io/part-of: ansible-gitops
    environment: prod
spec:
  refreshInterval: 30m
  secretStoreRef:
    name: hashicorp-vault
    kind: ClusterSecretStore
  target:
    name: ansible-vault-password
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/part-of: ansible-gitops
          environment: prod
  data:
    - secretKey: vault-password
      remoteRef:
        key: prod/vault-password
        property: vault-password

---
# ── SSH private key ───────────────────────────────────────────────────
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: ansible-ssh-key
  namespace: ansible-jobs-prod
  labels:
    app.kubernetes.io/part-of: ansible-gitops
    environment: prod
spec:
  refreshInterval: 12h
  secretStoreRef:
    name: hashicorp-vault
    kind: ClusterSecretStore
  target:
    name: ansible-ssh-key
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/part-of: ansible-gitops
          environment: prod
  data:
    - secretKey: ssh-private-key
      remoteRef:
        key: prod/ssh-key
        property: ssh-private-key
